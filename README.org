#+title: Chez Euler

  Stuff I use to solve Project Euler problems, mostly. Also some
  examples where pretty much all is done by writing the library
  procedures.

#+begin_src scheme :exports both :session euler
(import (chez euler))
(library-exports '(chez euler))
#+end_src 

#+RESULTS:
#+begin_example
(vector-swap! vector-modify! vector-dec! vector-inc! dec!
 inc! pop! push! for/range iaverage average =~
 ,*machine-epsilon* const flip curry compose cube square
 pandigital? digits->integer digit-sum digits digit-fold
 algorithm-P blum-blum-shub closed-segment-intersection
 open-segment-intersection _end _start _y _x segment point
 coefficients->polynomial p:singleton p:shift/scale p:shift
 p:scale p:^ p:+ p:* horner p:degree p:coefficient p:constant
 cf:sqrt g:convergents convergents *primes* s:pi s:cos s:sin
 s:e^x factorials binomials s:choose s:factorial s:accelerate
 s:euler-transform s:tails s:square s:convolve s:interleave
 s:negate s:derive s:integrate s:enumerate s:chunks s:cycle
 s:bind s:join s:append s:scale s:constant s:find
 s:drop-while s:take-while s:drop s:take s:filter s:map
 s:iter s:ref s:cdr s:cons omega Omega crt-system
 chinese-remainder-theorem Z/nZ* divides? coprime?
 inverse-modulo ax+by=gcd prime? totient divisors
 factorize-with-multiplicity factorize moebius-sieve
 totient-sieve primes-in-range primes)
#+end_example

